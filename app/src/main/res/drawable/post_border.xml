<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Background color -->
    <corners android:radius="8dp" /> <!-- Rounded corners -->
    <stroke
        android:width="1dp"
        android:color="#807E7E" /> <!-- Border color and width -->
    <solid android:color="#FFFFFF" /> <!-- Background color of the post -->
    <stroke
        android:width="2dp"
        android:color="#A0A0A0" /> <!-- Border color -->
    <corners android:radius="8dp" /> <!-- Rounded corners -->
    <solid android:color="@android:color/white" /> <!-- Background color -->
    <stroke
        android:width="2dp"
        android:color="#A9A9A9" /> <!-- Border color -->
    <corners android:radius="4dp" /> <!-- Corner radius -->
</shape>
